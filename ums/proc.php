<?php	// prodecural functions		function req($className)	{		$path = 'classes/' . $fieldName . '.php';		if (file_exists($path)) 			require_once($path);		else			echo "Class $className could not be loaded.";	}		DEFINE('ERROR_UNKNOWN', 0);	DEFINE('ERROR_DATATYPE', 1);	DEFINE('ERROR_MYSQL', 2);		function error($num, $obj)	{		die('			<p>An unexpected error occurred.</p><ul>			<li>Error code: ' . $num . '</li>			<li>Object: ' . get_class($obj) . '</li></ul>');	}		function make_permission($rAdmin, $rUser, $rGuest, $wAdmin, $wUser, $wGuest)	{		$value = 0;		if ($rAdmin) $value += 32;		if ($rUser) $value += 16;		if ($rGuest) $value += 8;		if ($wAdmin) $value += 4;		if ($wUser) $value += 2;		if ($wGuest) $value += 1;				return $value;	}	?>